name: Empty CI/CD

on:
  push:
    branches: [ main ]

jobs:
  do-nothing:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello CI/CD — this workflow does nothing"
